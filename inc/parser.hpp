#pragma once

class Parser {
    // sections, data
    // entry point

    Parser(const char path[]) {
        // checks
        // check file path
        // check that file is elf
        // etc...

        // read elf64 sections (.text, .data etc)
        // read entry_point
    }
};
